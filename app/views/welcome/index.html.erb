<div class="container welcome">
  <% if @local_resturants.nil? %>
  <div class="col-8">
    <h2 id="foode">FOODÉ</h2>
  </div>
    <div class="container">
      <div class="row">
        <h2>
          <i class="fas fa-search"></i>
          Search for your favorite food!
        </h2>
      </div>
      <div class="row">
        <h2>
          <i class="fas fa-map-marker"></i>
          See all the local menus!
        </h2>
      </div>
      <div class="row">
        <h2>
          <i class="fas fa-filter"></i>
          Filter by your favorite types or diet requirments!
        </h2>
      </div>
    </div>
    <hr class="my-4">
    <div class="signup">
      <p class="lead">
        <!-- <%= link_to('Sign Up', new_user_registration_path, class: 'btn btn-success btn-lg', title: 'Sign UP') %> -->
        <%= link_to('Subscribe', new_charge_path, class: 'btn btn-success btn-lg') %>

      </p>
    </div>
  </div>
<% else %>
  <%= render 'show' %>
<% end %>
